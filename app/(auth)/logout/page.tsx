import { redirect } from 'next/navigation';

export default function LogoutPage() {
  // 1. Get the session token from the cookie
  // 2. Delete the session that matches that token
  // 3. Send cookie deletion command

  return redirect;
}
